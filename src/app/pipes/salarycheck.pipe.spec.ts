import { SalarycheckPipe } from './salarycheck.pipe';

describe('SalarycheckPipe', () => {
  it('create an instance', () => {
    const pipe = new SalarycheckPipe();
    expect(pipe).toBeTruthy();
  });
});
